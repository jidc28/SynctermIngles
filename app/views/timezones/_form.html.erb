<%= form_for(@timezone) do |f| %>
  <% if @timezone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@timezone.errors.count, "error") %> prohibited this timezone from being saved:</h2>

      <ul>
      <% @timezone.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
